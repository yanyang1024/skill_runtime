# 合并之后，如何评估现有 Skill / Agent 并找到优化方向

依据：你最新的 `compare_the_ability.md`、此前的内网四个 Skill 说明与真实会话反馈。关于合并代码的规模、字节一致和回归结果，本文按你的反馈理解；没有拿到完整合并代码进行独立审查。本次只给实施建议，不改现有 Skill，不假定内网已具备完整 Agent 评测运行器。

**当前最值得推进的是：用合并后的会话入口找候选，补齐相关 Agent/Skill 定义和目标产物，再形成“具体证据 → 具体问题 → 小改动 → 对应验收”的能力级报告。** 完整 Agent 对照可以后补，已有信号分析和局部验证可以先产生结果。

## 导航

1. 对合并反馈的取舍
2. 先区分四种评估
3. Skill 与 Agent 各看哪些信号
4. 最小证据接口建议
5. 如何使用当前已有 Skill
6. 让报告直接指向可修改的位置
7. 最小对照环境怎样起步
8. 下一轮的实施顺序

## 1. 对合并反馈的取舍

### 1.1 保留主入口，按需求恢复能力

固定原始 OpenCode JSON 作为主输入，在你的平台内是合理的工程选择。解析事实、缓存、投影和人可读报告也有实际价值。代码从几百行增长到两千多行，本身不能证明过度设计；要看重复工作是否减少、事实是否可追溯，以及增加一个分析是否必须改很多层。

| 反馈中的变化 | 当前建议 | 为什么 |
|---|---|---|
| 原始 JSON 成为唯一主输入 | 继续保留 | 原始数据最丰富；暂时没有 MD 导入需求，就不必为了恢复旧功能而补回 |
| 事实层、缓存、投影已完成 | 直接复用 | 你已完成薄适配和回归，下一步不应再次重写 parser |
| 信号定义、标签边界和别名映射丢失 | **优先恢复** | 它们影响每条信号如何被解释，比恢复全部脚本更紧要 |
| 部门比较丢失 | 需要横向分析时作为可选分析恢复 | 当前主目标是具体 Skill/Agent 优化，不必每次都跑部门比较 |
| 任务边界候选丢失 | 先允许人工标一个目标片段，长会话多时再恢复脚本 | 会话不是任务，但也不必先建设完整自动切段器 |
| MD 解析、demo、界面元数据丢失 | 按实际使用场景恢复 | 它们与当前生产 JSON 分析的优先级不同 |
| 固定六步流水线 | 保留默认路径，允许复用已有事实/只重建分析结果 | 按需执行不一定要变成大量子命令或参数 |

建议恢复 references 作为判断依据，恢复 label_adapter 作为字段映射；不要只复制脚本。也不要再以代码目录是否完整作为“合并成功”的主要标准。

### 1.2 把几个过强结论改得更准确

| 反馈中的判断 | 建议改写 |
|---|---|
| 阶段一 100% 无新接口、零持续人工 | 基础取数与合并收尾大多能用现有资产完成；**深评 Agent 行为和产物质量，仍可能缺定义/文件证据**。字段与规则变化后也需要少量复核 |
| A ⊇ B，因此画像侧直接兼容 | 键覆盖是必要条件；单位、缺失值、聚合范围和语义还要一致。用少量代表样例确认即可，不要求大规模重测 |
| 已有 Skill 加载事件，可核对实际版本 | 加载名称证明接触；核对版本还需实际加载内容、指纹或与该次运行绑定的快照 |
| 所有阶段二工作都要等 C2 运行器 | 历史审计、题卡与验收定义可先做；脚本局部对照可用简单执行脚本。完整 Agent 新旧运行才需要更完整环境 |
| 阶段三全部是人工问题 | 授权与最终采用有人的职责；指纹匹配、加载核对、常见凭据检测等可以自动辅助，不能因此认定技术已齐备 |

此前我把“已有运行器”写成默认前提，这一点应修正：**按你现在的反馈，目前没有能直接产出完整配对 Agent 运行的能力。** 后面的路径按这个条件设计。

“有人工职责”也不等于每一步必须纯人工完成：Agent 可以草拟题卡、验收和经验候选，规则可承担确定检查；人工集中确认重要业务标准和争议项。没有确认时留候选，不阻塞所有观察。

## 2. 先区分四种评估，避免一起等运行器

| 评估层 | 回答的问题 | 最小依赖 | 可以形成的结论 |
|---|---|---|---|
| 定义与静态契约 | Agent/Skill 的描述、输入、权限、引用是否自洽？ | 配置、文档、脚本或引用文件 | 当前版本的明确问题或风险候选 |
| 历史执行审计 | 已发生的任务中，哪些承诺有证据满足/违反？ | 原目标、相关轨迹、适用定义、必要产物 | 这批已观察案例中的表现与缺口 |
| 局部行为验证 | 某个函数/校验器/参数模板的改动是否修复具体问题？ | 旧/新实现、固定夹具、明确断言 | 被验证范围内的局部改善或退化 |
| 完整 Agent 对照 | 整体任务在新 Harness 下是否更好？ | 可重建输入、隔离环境、模型/工具、两组实际运行 | 本批冻结任务上的增量效果 |

前三层可以先推进。历史数据中“加载了 Skill 的任务结果较好”仍是关联；要证明 Skill 增量，才需要相应对照。局部校验器修复有效，也不能直接外推完整 Agent 成功率已经上升。

评估单元可以小到“一次交接”“一次明确校验”“一个必须保留的条件”。只有问题跨越完整任务时，才使用整个任务作为单元。

## 3. Skill 与 Agent 各看哪些信号

第一批每个目标能力选少量普通样本与问题样本，例如各几条，允许 unknown。记录来源用户、任务家族和选择方法，避免把同一材料的反复尝试当作很多独立证据。不要把定向失败样本的比例写成总体失败率。

### 3.1 Skill：从接触走到契约，再看增量

| 想判断什么 | 最小证据 | 信号性质与边界 | 优化方向 |
|---|---|---|---|
| 是否在合适任务中加载 | 用户原目标、Skill 适用描述、加载事件 | 适配性通常需语义复核；加载次数本身可直接计数 | 收紧 description、补适用条件或路由提示 |
| 加载了什么内容 | 加载返回、引用/脚本事件、可核对版本 | 名称是接触证据；当前磁盘文件不等于历史版本 | 修加载路径、版本记录、资源索引 |
| 必要输入与约束是否齐全 | Skill 契约、任务输入、调用参数 | 字段/格式可规则查，业务充分性需判断 | 增加前置校验或必要澄清 |
| 必要步骤是否覆盖目标 | 可见工具调用、目标列表、校验报告 | 只检查影响正确性的必要步骤；允许等价路径 | 修具体脚本、参数或校验约定 |
| 结果是否满足承诺 | 原材料、输出、验收项 | 文件存在/写入成功不能证明完整和保真 | 补条件保留、覆盖映射、验证步骤 |
| 是否值得继续保留这段指导 | 同任务质量、总消耗、重复结果 | 需要有/无或旧/新对照；加载但没有专属 invoke 未必有问题 | 精简重复内容、拆分参考材料或保留有效步骤 |

若要查“该加载却没加载”，必须另取适用任务样本，不能只分析经 Skill 过滤得到的已加载会话。判断是否适用尽量依据用户输入，避免用最后产物反推标签后再评价 Skill。

### 3.2 Agent：优先看交接和状态传播

| 想判断什么 | 最小证据 | 信号性质与边界 | 优化方向 |
|---|---|---|---|
| 角色边界是否明确 | Agent 文档、有效权限/工具配置 | 静态说明可查；运行时是否执行还需事件 | 消除冲突说明、明确可做与应交接的条件 |
| 委派是否带全任务条件 | 父任务目标、委派参数、附件引用 | 字段可规则查，约束传递需语义核对 | 修改任务包模板或引用方式 |
| 子 Agent 是否给出可消费回执 | task 返回与回执契约 | 纯 JSON/必填字段是格式检查；completed 不是业务通过 | 简化并固定必要回执字段 |
| 上游是否处理 warning/fail | 子回执、后续父 Agent 行为 | 接收回执不等于正确使用；缺后续轨迹则未知 | 改分流、停止或补查条件 |
| 用户修改是否传到下游 | 修改前后目标、计划/job/产物 | 比较时点和版本后才判断漂移 | 修变更传播、失效标记或重新生成条件 |
| 分工是否带来收益 | 同任务、同预算下的结果和交接消耗 | 角色多、委派多都不是收益证据 | 简化重复角色，保留有用的上下文隔离 |

以前课程案例的 builder/verifier 配对，适合检验回执、目标指针和 warning 消费。没有子会话轨迹时，只能判断主轨迹可见部分；不能据此认定校验器实际执行或没有执行。平台已有子会话 ID 时，优先复用会话查询能力补对应轨迹，并保持来源关系。

Agent 配置有全局、项目、角色和会话覆盖时，单个 agent.md 只是定义的一部分。需要分清“这份文件写了什么”与“本次运行实际生效了哪些约束”。不要请求或依赖模型隐藏思维链，外部行为证据已经足以支持很多诊断。

### 3.3 对新增 extras 的四个信号保持不同证据级别

| 新信号 | 可以直接说什么 | 不能直接升级成什么 |
|---|---|---|
| exit 非零候选 | 可见记录显示某退出码，需要结合命令契约解释 | 非零一定是工具故障；例如某些查找命令以非零表示未命中 |
| 用户点名文件 | 用户引用/要求某个文件 | 文件存在、被正确使用或最终被采用 |
| 指纹三档 | 在已定义表示和来源下是否匹配 | 工具 I/O hash 是完整文件 hash；去空白/casefold 等于语义一致 |
| 紧邻恢复判定 | 错误后附近出现一次成功或输入变化 | 原问题已经解决、未发生人工介入、任务已经完成 |

这些信号适合提高复核命中率。先验证是否确实减少无效复核，再考虑是否能成为自动判定条件。

## 4. 你可以补接口，但优先补“证据可达性”

**值得补的两类能力是：会话关联的 Harness 定义、工作目录里的目标文件。** 它们直接帮助判断“应该怎么做”和“实际上做成了什么”，无需等完整运行器。已有 Skill 下载接口继续复用。

先为本轮要评的一个 Agent/Skill 提供文件导出也可以；当手工收集反复成为瓶颈时，再将同样的数据契约开放为接口。优先扩展现有查询服务，不必另建平台。

### 4.1 Agent / Harness 定义接口

以下是建议契约，不代表平台已有路由。

`GET /sessions/{session_id}/harness-evidence`

至少返回：

- 可见 Agent 名称及其所属用户/项目/配置范围，避免同名不同配置混在一起。
- agent.md、相关 AGENTS.md、权限/工具配置、关联 Skill 的资源引用；已有渠道可读取的 Skill 只给引用即可。
- captured_at、各文件的内容指纹、来源层级，以及是否与该次运行绑定。
- 没有历史快照时明确 `evidence_basis=current_snapshot`；有运行时绑定证据才标 `observed_at_run`。重建结果另标 reconstructed，并说明依据。

第一版不必让接口自动合成完美的“有效 Prompt”。先返回相关层级及来源；解析优先级未确认时，报告配置冲突候选。后续可在平台实际组装上下文的位置记录已生效文件/内容指纹。

**agent.md 的 hash 不代表整个 Harness 版本。** 若改动涉及 Skill 脚本、参考文件或权限配置，至少将本次相关文件纳入版本记录。

### 4.2 工作目录文件接口

建议用“列资源 → 按引用读取”两步：

| 建议路由 | 最少返回内容 | 用途 |
|---|---|---|
| GET /sessions/{session_id}/artifacts | artifact_ref、相对路径、类型、大小、取证时点、可用版本；有来源事件则保留引用 | 找到任务输入、job、校验报告和最终产物 |
| GET /artifacts/{artifact_ref}/content | 指定版本内容或限长预览；明确完整/截断及读取范围 | 程序检查或给语义 Judge 提供必要片段 |

文件列表中的 input/output 角色，只有平台本身记录时才当事实；仅按目录/后缀猜测的标为 role_hint。读取后得到的“当前文件”，不能自动代表历史那轮输出。

对指纹只要求把来源说清：完整文件字节 hash 与返回片段 hash 分开。预览有包装、截断或编码转换时，不用预览 hash 证明原文件相同。最好让资源引用对应取证时的固定内容；若读取时已变化，则返回版本变化信息，避免拿旧元数据配新内容。

接口按已有用户/租户权限限定在关联工作目录，使用资源引用而非任意服务器绝对路径；凭据和认证配置不送入评测材料。此处是对这两个具体读取接口的边界要求，不需要把全面安全改造设为离线分析的前置工程。

### 4.3 暂时不用建设的能力

暂缓全服务器文件索引、自动反推全部历史版本、完整事件总线、通用多 Agent 实验调度器。新增只读接口也不能凭空补出旧运行的快照；旧数据留未知，从新增数据开始记录即可。

如果观察到加载事件但没有内容/版本证据，E1 最多完成“上传旁录 + 后续同名加载关联”。上传成功、文件存在、加载名称相同都不足以自动填写 harness_loaded_confirmed=true。

## 5. 如何使用当前已有 Skill

聚焦当前目标，默认只走这一小段：

**合并后的 session-usage-analysis → 相关定义/文件取证 → harness-diagnose。**

按需要继续：

| 当前需要 | 接着用什么 | 暂时不用强加什么 |
|---|---|---|
| 查已有 Skill 内容 | skills-manager 的查询/下载 | 上传、整个平台技能同步 |
| 判断 Agent/Skill 契约与历史执行 | diagnose + 程序检查/必要的语义评判 | 完整旧/新 Agent 运行器 |
| 把一个问题变成复用验收题 | eval-curate | 先凑齐大规模 train/val/test |
| 局部修复已有成对结果 | harness-compare 或简单配对汇总 | 全业务 Agent 重放 |
| 有完整重放条件后比较整体效果 | harness-compare + 实际运行器 | 用两次纯文本 v1 调用冒充完整执行 |
| 沉淀已验证操作经验 | experience-screen | 自动把通过结果写入全局记忆 |

total-use 和完整用户画像作为可选背景，暂时不放到每次能力评估的必经路径。部门/岗位用于选样、适用范围和结果切片，默认不给 Judge 用来猜输出质量。

**仍需核对一处薄适配**：你已经完成的是 merged facts → observe。它不自动意味着 merged facts 已兼容 diagnose 的 events/receipt 结构、Judge jobs 或 compare 的 runs。只补各阶段实际使用的字段映射，不重写解析器；缺原始参数/回执就回到 raw JSON，而不是从报告摘要推回“事实”。

历史审计也可以使用语义 Judge：一次只核对一个主要承诺，提供原目标、相关定义、必要轨迹和产物，输出判断、证据与缺口。身份、旧评语和修复者猜测不作为正确性依据。对 Judge 尚未校准的维度，先作为复核助手；不自动成为发布门槛。

## 6. 让报告直接指向可修改的位置

不建议继续增加一个综合价值分。每个目标 Agent/Skill 先出一张能力报告：

1. **评的是什么**：名称、所属范围、版本证据、适用任务。
2. **看到了多少**：查询/下载/解析数、正常与定向样本、独立用户/材料家族、资料缺口。
3. **发现什么**：事实和候选分列；通过、失败、未知使用相同范围和分母。
4. **最值得改什么**：少量建议，每条绑定可见证据、具体位置和验证方法。

每条建议用下面的短卡片即可：

| 项目 | 内容要求 |
|---|---|
| 观察 | 哪次调用/哪段材料/哪个字段出现什么现象 |
| 契约 | 哪条明确要求适用于它；若只是优化偏好需标注 |
| 状态 | confirmed / hypothesis / insufficient_evidence |
| 其他解释 | 输入不足、工具返回异常、版本不一致、轨迹缺失等 |
| 修改位置 | Agent 交接模板、Skill 某一步、具体脚本/函数或工具错误契约 |
| 一个小改动 | 限定本轮改动，避免一次重写整套 Skill |
| 验证与退化风险 | 用什么正常/反例证明修复，哪些原有任务可能受影响 |

优化建议的证据可以分三级：确定的可复现缺陷直接建议精确修复；多个相关案例支持的模式提出待验证改动；证据不足时优先建议补哪一条记录。不要用很精细的小数分数掩盖这三者的区别。

例如，“该 Agent 质量低”难以行动；“子 Agent 返回 warning 后，父 Agent 在可见轨迹中仍直接提交，且契约要求先处理该 warning”就能指向一条明确分流规则。后续再用正常回执与 warning 回执验证它。

## 7. 最小对照环境怎样起步

### 7.1 先做脚本/校验器级对照

你反馈中的 f96126 校验器缺陷，是一个候选起点；这里没有独立重现该缺陷。先取到相关程序与输入，在副本上确认现象。

最小环境可以只是一个 Python 驱动：为旧/新实现各准备独立临时目录，放入同一初始夹具，执行已审阅命令，记录退出码、stdout/stderr、报告和产物指纹。每次重置输入，设超时，保留所有尝试。这不需要先建 Agent 调度服务或 Docker。

先放几种明确用例：合法目标、缺失目标、未被索引目标、正常应拒绝的错误输入。验收关注是否覆盖目标、是否以正确原因拒绝以及正常任务是否被误伤。崩溃不能冒充正确拒绝，零检查目标不能冒充通过。

这已经是一个局部运行器。无须把“完整 Agent 运行器尚不存在”当作这类工作的阻塞点。

### 7.2 需要验证整体收益时，再接完整 Agent

最低能力是：从冻结初始输入启动独立会话，指定旧/新 Harness，确认实际加载内容，执行同一任务，导出轨迹/产物/资源使用。使用平台已有隔离机制即可；每组独立工作目录，防止后一组读到前一组生成的答案或修改后的状态。

早期可以人工分别启动两组，自动收集与比较；不必先暴露通用执行 API。开始频繁重复实验时，再封装相同流程。

固定模型部署、工具/数据环境、输入和预算；外部 API 状态无法固定时标不可比。重复数预先约定，计入失败与重试，不挑最好一次。定义/脚本单元检查通过后，完整 Agent 实验仍可能发现交接和执行路径问题，两类结论分别保留。

当前 compare_runs 示例针对固定模型、不同 Harness 的结果；不要为了套格式给局部脚本虚构 model_id，或把文件存在伪装成已加载。局部执行先使用明确标注 scope=validator_unit 的简单配对汇总，确有需要再给现有汇总器增加对应类型。

## 8. 下一轮的实施顺序

| 优先级 | 具体工作 | 停下来判断是否完成的标准 |
|---|---|---|
| P0 | 恢复信号定义/标签纪律，复用当前 facts、缓存与投影 | 每条关键指标能解释输入、范围、分母和 unknown |
| P0 | 选一个 Skill 和一个 Agent，获取少量相关会话与定义 | 能区分接触证据、契约、结果和当前缺口 |
| P1 | 补目标 Agent 文档和产物文件的读取能力 | 能拿到判断所需材料，知道取证时点和版本是否与运行绑定 |
| P1 | 用 diagnose 形成少量有具体位置的优化卡 | 每条卡有证据、替代解释、一个小改动与验收办法 |
| P1 | 对最确定的一处脚本/校验器问题做局部对照 | 有旧/新结果，正常用例与反例都被有效检查 |
| P2 | 接完整 Agent 运行、扩真实评测题与 Judge 校准 | 能回答整体改动是否更好、是否退化及适用范围 |

如果手工导出已经能满足首批取证，P1 接口可稍后补；如果定义/文件完全拿不到，接口就是下一步优先项。选择由当前缺哪条证据决定。

关于凭据和安全审查，保留与具体改动相称的检查及现有发布要求；专业审查可能有必要，但不宜把全量专业扫描设置成每次只读信号分析的统一前置条件。

本轮交付可以只有三样：**一张目标能力报告、几条可行动的优化卡、一组确实执行过的局部对照结果**。先证明信号能帮助改好一个实际问题，再扩大到更多部门、能力和自动化阶段。
