# 本次验证范围

验证日期：2026-09-05。所有输入均为虚构样例，没有公司真实会话、项目文件或内网访问。

已验证：

- `run_demo.sh` 完整运行：生成样例 → Markdown 适配 → 导入 → 同数据重复导入 → 人工确认样例 → 全量案例与报告 → 分组数据导出 → 合成候选 → 冻结 benchmark → 两个 MOCK responder → 比较报告。
- 32 条会话样例首次导入 32 条，再导入 32 条全部 unchanged；缺时间/结果/usage 的记录保持缺失语义。
- 6 道固定输入 benchmark 样例能冻结和比较，报告明确标为 MOCK，不是实际模型分数。
- 10 项回归检查通过，覆盖：幂等/保留旧案例、来源变化导致复核失效、感谢不能代表完成、Markdown 代码围栏、请求去重、资产租户/版本/时间身份、跨集重复、历史 holdout 禁止转训练、合成来源继承、JSON 类型匹配、缺失结果不算平局等相关不变量（部分测试含多个断言）。
- 可选 CPU 分类训练真实运行完成，使用 scikit-learn 1.8.0、18 条虚构训练样本和 6 条虚构 dev 样本；未使用真实公司数据或 GPU。dev 满分不能证明实际泛化；默认 0.75 阈值下自动选择覆盖为 0，示例也不会把低分数样本强行路由。
- shell 语法与 Python 编译检查完成；文档链接检查完成。

未验证：

- 你们真实的 OpenCode 导出格式、所有版本 parts/info 包装、内网 API、账单/HR 组织接口。
- 真正模型的 Chat Completions 调用、生产 API 回放、完整工具执行 benchmark。
- GPU / LoRA 训练、依赖兼容、离线模型权重与超参数效果。文档中的 LoRA 为伪代码。
- 真实业务收益、总体完成率、工时节省或模型泛化；示例中的确认、费用与采用数都是虚构数据。

建议迁入公司环境后的第一项验证：选择 10 条允许使用的真实会话，人工核对角色、时间、usage 与输出证据；再运行相同回归检查。不要直接把 demo 的数字替换成平台总体结论。
