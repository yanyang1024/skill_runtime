# 协作模式

1. 按实际输入类型分派。给出自包含任务包，不能只说“分析一下”；遇到 PDF 等未支持格式，先要求提供可读取文本或明确可用后端。
2. 检查提取返回字段完整性。对每个可用分支的候选结论发起独立核对，仅传任务要求、源引用、候选结论和验收口径。
3. 验证者必须覆盖该分支全部候选 id。FAIL 时将具体问题交还原执行者；本示例允许一次修正后再开新核对会话，仍不通过则报告失败。BLOCKED 时列明缺口，不盲目重试。
4. 汇总分别通过核对的结论；任一请求分支未完成时明确部分完成，不声称整项通过。无有效回传按未完成处理。简单解释可直接回答，不为了形式派单。
