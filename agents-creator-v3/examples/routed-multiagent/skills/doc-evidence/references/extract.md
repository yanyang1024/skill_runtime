# 提取模式

读取 goal 和 sources，确保任务范围与输入齐全。

文本模式：读取真实 Markdown／纯文本，提取与目标相关的事实，不超过任务约定数量。每项事实附原始路径和定位信息，分清“目标”“已完成”“待办”等语义。实际内容不足时报告材料限制，不填满数量。

视觉模式：先确认实际可见图像。按区域观察、记录可辨识内容与不确定部分；不能因名称推断图片内容，不能将模糊文字当确定事实。读取能力缺失回传 BLOCKED。

提取完逐项核对事实与引用，按共享契约回传。此处的自查仅保证交接质量，独立核对仍由验证者完成。
