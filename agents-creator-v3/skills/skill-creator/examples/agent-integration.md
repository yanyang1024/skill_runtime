# 为 agent 创建配套 skill

需要看完整交接时读取 [联合创建示例](../../agents-creator/examples/agent-with-skill.md)。这是本分发包提供的可选例子；若 skill-creator 被单独安装、兄弟目录不存在，按本 skill 主流程继续，不将缺少例子当作方法阻塞。

本例只用同一 agent 的 skill 加载切换，保留当前设计与授权范围。你负责方法与其依赖，完成后向现有流程回交；不要再次触发整个 agent 创建任务。
