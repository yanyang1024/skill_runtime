# v2 验证说明

## v2.2 本轮有限检查

2026-09-06：新增 5 个针对性用例，加上既有用例共 29 项通过。覆盖缺失与空输入、成功状态下的断言失败、开发阶段候选不被淹没、仅加载/版本不明、采用确认不覆盖任务标签及已见失败调试入口。一轮 32 会话虚构离线 demo 完成，SFT 仍为 0；shell 入口语法通过。未重训路由、未调用真实模型、未连接内网，不以此验证你报告中的真实效果。后续历史验证记录保留。

## v2.1 本轮基本检查

在既有 22 项检查上，只增加任务图谱的两个边界用例：过期/未知标签不产生真值、同源去重及已知 train 排除。共 24 项通过；一次虚构 demo 完成，并输出任务分布、候选来源和行动看板。shell 入口语法检查通过。未重新进行 GPU、真实模型或内网接口试验；以下 v2 记录作为历史验证保留。

验证日期：2026-09-06。执行环境 Python 3.12.13；核心脚本通过 Python 3.9 语法兼容检查。未在 Python 3.9 解释器运行。可选 CPU 环境 scikit-learn 1.8.0。

## 已完成

- 22 项回归检查通过：保留 v1 的幂等、来源版本、引用/时间、跨集重复与缺失评分检查；新增 AUTO 分流、弱标签/SFT 隔离、组织隔离可选、测试断言负例、上传/hash 歧义、多租户、加载不等于调用、跨组织请求归因、提示原文保留、低分透传、路由答案泄漏拒绝、分页/owner 审计及 summary dict 处理。
- 离线 run_demo.sh 完成：32 个虚构 session，重复导入 unchanged=32；10 个虚构人工确认 + 1 个 AUTO 候选，AUTO 不计人工采用。数据导出 18 train / 6 dev / 6 holdout 路由记录；默认 SFT train/dev 均为 0；另产生 5 个未批准合成候选。
- 组织能力示例输出 8 个工具分组、5 个能力分组、3 条产物关系，包括明确来源上传、成功读取、多个来源的同字节上传候选。样本故意很小，只验证口径与输出。
- CPU TF-IDF+LR 实际拟合 18 个虚构样本，对 6 dev / 6 holdout 运行三基线比较。TF-IDF 两集 F1=1，但默认 0.75 阈值覆盖均为 0；这只说明流程能执行，不能用来证明真实泛化或推荐阈值。指标完整保存在 examples/router_metrics_demo.json。
- 6 个虚构路由题跑两个 MOCK responder，验证成对新增通过、退步、缺失与切片报告。
- 另用 4 个独立虚构计算任务、4 种提示策略、每题 2 次，共 32 个 MOCK 结果检验提示对照流程。分类器在此使用阈值 0 强制覆盖分支，仅供代码验证；没有实际模型调用。假 responder 输出 intent，因此故意不通过计算目标；没有声称得到提示效果提升。
- run_weekly.sh 无人工确认文件也能完成导入、价值报告与组织/资产诊断。
- shell 入口做语法检查；交付 zip 的 SHA256SUMS 覆盖源文件与说明，压缩包不含数据库、pickle 模型、临时运行目录或 Python 缓存。

## 未执行/不能据此确认

没有访问公司 API、真实会话、GPU/LoRA 或真实模型网关；没有重算你的 1226 会话、15,863 请求、各部门失败率和真实路由分数。API 检查使用注入的假响应，内网分页、过滤与 owner 语义仍需按实际契约适配。没有部署提示路由或建立定时任务。

没有实现完整 Agent 工具回放、语义去重、程序 golden 的通用真值验证器、实际业务采用推断或货币收益估算。程序参考可信度依赖你给出的验证器及输入快照；脚本中的 approved/validator_ref 是可追溯声明，不能使错误答案自动成为真值。

## 迁入公司后的最短核对

沿用已有导出，核对一个查询的分页/详情/owner 与一个高失败 skill 的分母及阶段，再检查少量已知产物的来源关联。先对齐你的 taxonomy 和短提示，在独立下游 dev 上选策略/阈值，最后跑冻结 holdout。没有持续人工资源时，未知保持未知，自动分析继续。
